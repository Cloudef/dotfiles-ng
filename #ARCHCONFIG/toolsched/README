source /arch/toolsched/toolsched-env
needs schedtool

/arch/toolsched/toolsched-lib
   provides helpful functions

/arch/toolsched/sched_<sched>
   premade template files you can just ln -s

# Set current process scheduler parameters
toolsched_<sched>

# Launch binary with the current script's name
toolsched_launch [binary arguments]
